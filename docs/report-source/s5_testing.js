const H = require('./report.js');
const { P, Rich, H1, H2, H3, Bullet, Num, Code, Tbl, TableCaption, GREY } = H;

module.exports = [
  H1('5. Testing'),
  P('This section corresponds to the Phase 4 deliverable. Testing was performed at two levels: an automated suite exercising the application through real HTTP requests, and manual verification of the complete demonstration path in a browser.'),

  H2('5.1 Testing Strategy'),
  P('The automated suite runs against an in-memory SQLite database, rebuilt for each test, so tests are fast and cannot contaminate the demonstration data. Tests exercise the application through its routes rather than calling methods directly, which means routing, middleware, policies, validation, the service layer and the database are all covered by the same assertion.'),
  P('One configuration choice deserves note. The suite runs with Eloquent lazy loading disabled:'),
  Code([
    '// Catch N+1 queries in the test suite, but stay permissive at runtime so',
    '// a missed eager-load degrades into extra queries rather than a 500.',
    'Model::preventLazyLoading($this->app->runningUnitTests());',
  ]),
  P('With this enabled, any relationship the code forgot to load in advance throws an exception rather than silently issuing an extra query per row. A performance regression therefore fails the build instead of quietly appearing in production.'),

  H2('5.2 Automated Test Suite'),
  P('The suite comprises 73 tests making 249 assertions, and completes in approximately three seconds.'),
  Tbl(['Test class', 'Tests', 'Coverage'], [
    ['AuthenticationTest', '9', 'Sign-in, per-role landing pages, wrong password, inactive accounts, mid-session deactivation, sign-out, guest redirection'],
    ['RoleAccessTest', '9', 'Cross-portal refusals in every direction, and the ownership policies for sessions, classes and student records'],
    ['AdminManagementTest', '10', 'CRUD, duplicate rejection, paired account creation, timetable capture, bulk enrollment, semester activation, password reset, self-deactivation guard'],
    ['AttendanceMarkingTest', '7', 'Register saving, re-saving, roster tampering, close-marks-absent, credential retirement, empty-roster guard'],
    ['QrCheckInTest', '14', 'Valid, expired, rotated and unknown tokens; non-enrolled and duplicate check-in; late window; typed-code path; kiosk polling and its authorisation'],
    ['AttendanceReportTest', '11', 'Percentage arithmetic, late policy both ways, excused handling, open-session exclusion, at-risk ordering, single-query assertion'],
    ['CsvExportTest', '6', 'Download headers, byte-order mark, at-risk filtering, per-role export authorisation'],
    ['PagesRenderTest', '7', 'Every screen in all three portals renders with realistic data'],
  ], [2300, 900, 6160]),
  TableCaption('Automated test suite by class'),
  Code([
    'PS C:\Users\Rakze\Desktop\Assignment> php artisan test',
    '',
    '   PASS  Tests\Feature\AdminManagementTest',
    '   PASS  Tests\Feature\AttendanceMarkingTest',
    '   PASS  Tests\Feature\AttendanceReportTest',
    '   PASS  Tests\Feature\AuthenticationTest',
    '   PASS  Tests\Feature\CsvExportTest',
    '   PASS  Tests\Feature\PagesRenderTest',
    '   PASS  Tests\Feature\QrCheckInTest',
    '   PASS  Tests\Feature\RoleAccessTest',
    '',
    '  Tests:    73 passed (249 assertions)',
    '  Duration: 2.90s',
  ]),
  Rich([{ text: 'Figure 4: Automated test run.', italics: true, size: 18, color: GREY }], { after: 220 }),

  H2('5.3 Test Cases'),
  P('The table below records the fifty principal test cases, each traced to the requirement it verifies. All were executed against the finished system.'),
  Tbl(
    ['ID', 'Req.', 'Test case', 'Input', 'Expected result', 'Status'],
    require('./s5b_cases.js'),
    [660, 720, 2000, 1900, 3200, 880],
  ),
  TableCaption('Test cases, traced to requirements'),

  H2('5.4 Defects Found and Fixed'),
  P('Manual verification of the demonstration path uncovered three defects that the automated suite had not caught, because each concerned behaviour the tests were not yet asserting. All three were fixed and are now covered by tests.'),

  H3('Defect 1: Related counts silently rendering blank'),
  Tbl(['Aspect', 'Detail'], [
    ['Symptom', 'The Classes column on the student list, and the roster and session counts on the lecturer and class-section lists, all rendered as empty cells. No error was raised.'],
    ['Cause', 'The queries called withCount to add a counting subquery, then called select to restrict columns for a join used for sorting. In Laravel, select replaces the selected columns rather than adding to them, so the counting subquery was discarded. The view then read a property that did not exist, which Eloquent returns as null, and a null renders as an empty string.'],
    ['Why it was missed', 'The page still returned HTTP 200, so a test asserting only that the page renders could not detect it. The defect was visible only by reading the rendered output.'],
    ['Fix', 'The join used for sorting was replaced with an ordering subquery, which needs no select and therefore cannot clobber the count. Applied to four controllers.'],
    ['Prevention', 'This pattern is now recorded as a project convention, so future code does not reintroduce it.'],
  ], [1700, 7660]),
  TableCaption('Defect 1'),
  Code([
    '// Before - the count is silently discarded by select()',
    '->withCount([\'enrollments\'])',
    '->join(\'users\', \'users.id\', \'=\', \'students.user_id\')',
    '->orderBy(\'users.name\')',
    '->select(\'students.*\')',
    '',
    '// After - ordering by subquery keeps the count intact',
    '->withCount([\'enrollments\'])',
    '->orderBy(User::select(\'name\')->whereColumn(\'users.id\', \'students.user_id\'))',
  ]),

  H3('Defect 2: Ambiguous column reference'),
  Tbl(['Aspect', 'Detail'], [
    ['Symptom', 'The roster management screen returned HTTP 500 with the database error "ambiguous column name: id".'],
    ['Cause', 'The candidate-student query excluded students already enrolled using an unqualified column name. Once the users table was joined for sorting, both tables carried a column of that name and the database could not decide which was meant.'],
    ['Why it was missed', 'The screen had no automated test at the time. It was found by a test that walks every screen in the system with realistic data.'],
    ['Fix', 'The column reference was qualified with its table name, and the sorting join was removed in favour of an ordering subquery as in Defect 1.'],
    ['Prevention', 'The page-rendering test now covers every screen in all three portals.'],
  ], [1700, 7660]),
  TableCaption('Defect 2'),

  H3('Defect 3: Lateness judged against the wrong clock'),
  Tbl(['Aspect', 'Detail'], [
    ['Symptom', 'A check-in made at 10:28 local time for a class starting at 08:00 was recorded as present rather than late.'],
    ['Cause', 'The application was running in UTC while the timetable stores local wall-clock times. The server believed the time was 03:28, which is before the class had started, so the check-in was correctly early by its own reckoning but wrong by the clock on the classroom wall.'],
    ['Why it was missed', 'Every automated test constructs its times relative to the current moment, so the tests were internally consistent regardless of timezone and could not detect the mismatch. It surfaced only when a check-in was performed against seeded data using fixed timetable times.'],
    ['Fix', 'The application timezone was set to the university local time, and the reasoning recorded in configuration so it is not reverted by accident.'],
    ['Prevention', 'The timezone is now an explicit environment setting with a comment explaining why it must match the timetable.'],
  ], [1700, 7660]),
  TableCaption('Defect 3'),
  P('A fourth issue arose during development rather than testing: MySQL rejected an automatically generated index name for exceeding its 64-character identifier limit. Five constraints were given explicit short names in the migrations.'),

  H2('5.5 Manual Verification'),
  P('The complete demonstration path was executed in a browser against the seeded database.'),
  Num('Signed in as administrator. The dashboard reported 60 active students, 6 lecturers, 10 class sections and an overall attendance rate of 91.1% across 5,959 countable marks. The at-risk panel listed seven students, worst first.'),
  Num('Signed in as a lecturer. The portal showed only that lecturer two assigned classes and the single session scheduled for that day, confirming the scoping works.'),
  Num('Opened the day session. The system issued a token, redirected to the projection screen, and displayed a scannable QR code, the code UFV365, a live countdown and counters reading 0 of 30.'),
  Num('Signed in as an enrolled student. The dashboard detected the open session and offered a check-in shortcut.'),
  Num('Submitted the six-character code. The system confirmed the check-in as present and the record appeared immediately in the student recent-attendance list.'),
  Num('Returned as the lecturer and opened the marking register. All 30 students were listed, the late threshold was shown as 08:15, and the student who had checked in carried a Code self check-in badge.'),
  Num('Closed the session. The system reported that 29 students who never checked in were marked absent, and the self check-in was preserved as present.'),
  Num('Attempted to reach an administrator page while signed in as a lecturer. The custom access-denied page was shown with a route back to the correct dashboard.'),
  Num('Downloaded the at-risk report as CSV. The file arrived with the correct content type, a dated filename, a UTF-8 byte-order mark and 28 rows ordered worst first.'),
  P('The behaviour observed matched the expected result in every case.'),
];
